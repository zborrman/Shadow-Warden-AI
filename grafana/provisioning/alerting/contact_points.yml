apiVersion: 1

# Slack + email contact points are configured via Grafana UI (Alerting → Contact points)
# to avoid provisioning crashes when env vars are not set in the container.
contactPoints: []

policies: []
