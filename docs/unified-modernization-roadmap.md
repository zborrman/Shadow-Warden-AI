# Unified Modernization Roadmap — reconciling the two parallel plans

**Date:** 2026-07-12 · **Purpose:** merge the two concurrently-running modernization efforts into one registry so they stop colliding on shared files and on "Phase N" numbering.

## Why this doc exists

Two plans have been running in parallel, each numbering its work "Phase 1…7/8". In commit messages and reviews "Phase 5" has meant two different things. They also touch overlapping files (`causal_arbiter.py`, `staff_dispatch`, `net_guard`, the data layer). This document is the single source of truth: two **named tracks**, one status table, explicit conflict-ownership.

| Track | Source doc | Scope | Prefix |
|---|---|---|---|
| **A — Security Remediation** | `MODERNIZATION_PLAN.md` | Audit findings: auth, SSRF, IDOR, GDPR, invariants, DB-layer, CI hardening | `SR-*` |
| **B — Deep-Eng / Math** | `docs/modernization-plan-v8.md` | TDA, MAESTRO/reputation, Causal calibration, GSAM, embeddings, data-layer, runtime isolation | `DE-*` |
| **C — FinOps / Monetization** | `docs/fintech-development-plan.md` | Billing math (wallet unification), real-time cost rating, margin-aware routing, capacity/SLO math, reliability-as-revenue, growth accounting | `FM-*` |
| **P — Platform Hardening** | `docs/deep-analysis-2026-07-27.md` | Build determinism, packaging/image split, config seam, code shape, API-surface reduction, doc coherence, debt burn-down | `P-*` |
| **M — Marketplace Integrity** | `docs/marketplace-modernization-plan.md` | M2M marketplace request path: **30 of 55 cluster write routes carry no auth dependency** — negotiation authn + offer-signature verification (MP-1), ACP/agentic-commerce write auth (MP-8), injection-guard consolidation, live-gate coverage, dead `mp_*` projection, capability-manifest honesty | `MP-*` |

> **Rule:** commit messages and PR titles carry the track prefix (`SR-1.4`, `DE-5`), never a bare "Phase N".

---

## Track A — Security Remediation (from `MODERNIZATION_PLAN.md`)

| ID | Item | Status | PR |
|---|---|---|---|
| SR-0 | CRITICAL marketplace SQL | ✅ merged | #148 |
| SR-1.2/1.3 | Router auth (staff/secrets/red-team/doc-intel) + admin fail-closed | ✅ merged | #148 |
| SR-1.1 | Tenant tier from billing plan, not `X-Tenant-Tier` header | ✅ merged | #149 |
| SR-1.4 | `/gdpr` router auth (unauthenticated erasure closed) | ✅ merged | #154 (1c920552) |
| SR-1.4b | GDPR IDOR tenant-ownership match | ✅ done — own-tenant-or-`X-Admin-Key` on `purge_tenant`/`audit`; bulk `purge/before` now admin-only. Policy fork resolved: self-service default + operator override. |
| SR-2 | 8 SSRF sinks through `net_guard` + no-redirect | ✅ merged | #148 |
| SR-2.3 | net_guard validated-IP pinning (TOCTOU/DNS-rebind) | ✅ complete — `resolve_validated_ips()` + `build_pinned_url()`/`send_pinned()`/`send_pinned_async()` in `warden/net_guard.py` dial the validated IP directly while sending the real hostname as `Host` header + TLS SNI (cert still verifies against the real name — no TOCTOU DNS-rebind window between validate and connect). Wired into 8 live callers: `webhooks/engine.py`, `communities/federation.py`, `communities/notifications.py`, `communities/data_pod.py`, `business_community/agentic_commerce/ucp.py`, `billing/usage_budgets.py`, `workers/probe_worker.py`. `test_net_guard_pinning.py` (26 tests) includes `test_real_host_tls_sni_roundtrip` — network-gated, skips offline rather than failing, proves the roundtrip against a real host when network is available. This row was stale, same as DE-7 above — found via code verification, not assumed from the row's own text. | — |
| SR-2.4 | CORS `/ext/*` allowlist + refuse `CORS_ORIGINS=*` with credentials | ✅ merged | (SR-7/8 batch) |
| SR-3 | Correctness: Semantic params, STIX race, ZSET, OpenAI URL, stream-unmask, priv-esc, blocking-I/O | ✅ merged | #148 |
| SR-3.8 | Remove dead `_collect_or_emit` | ✅ merged | #154 (1c920552) |
| SR-4 | Invariants: SAR pre-screen, A2A boundary, GDPR-logs, refund key, Settings secrets | ✅ merged | #148 |
| SR-5 | **DB-layer consolidation** | ✅ delivered under DE-6 (`WARDEN_DATA_DIR`/`data_path`, `/tmp` sweep, `db/ddl_registry.py` DDL-once) — C2 as decided |
| SR-6 | Fail-open observability | ✅ done — `record_failopen()` + `warden_stage_failopen_total` + FAILOPEN-01 ratchet (`test_no_new_counterless_failopen`, baseline 200) |
| SR-7 | CI/supply-chain hardening | 🟡 gates in — bandit (HIGH), **semgrep** and **gitleaks** all GATING at a verified-clean baseline; SBOM/SLSA/Trivy/pip-audit/ZAP/Nuclei already existed. **SR-7.2 in progress: 79%→83%** (`--cov-fail-under=83`, 2026-08-01) — closed `staff/boundaries.py`, `staff/dispatcher.py`, `sac/guard.py`, `auth/router.py`, `auth/saml_provider.py` to 100% (Redis-backed branches, SAC blocked/fail-open paths, full cookie-auth endpoint suite, real python3-saml settings/metadata/login-URL + a faked `OneLogin_Saml2_Auth` for `process_response()` branches — python3-saml is a pinned dep, `constraints.txt`). Found and **deleted** `warden/auth/saml.py` (dead code: superseded by `saml_provider.py`, unwired from `main.py`, zero non-test importers) plus its 7 orphaned `Settings` fields and the stale README v4.14 file reference — `test_xxe_hardening.py` trimmed to the two live defusedxml call sites. 85% still open — remaining gaps are non-security (`main.py` 794 missing lines, `marketplace/`, `communities/`, `syndicates/` routers). **SR-7.3 re-measured 2026-08-31**: mutation testing is no longer the untouched item this row described — `secret_redactor` 33 → 19 (#277), `auth_guard` 89 → ~51 (#280), `x402_gate` 259 → 193 (#284/#288) and `causal_arbiter` **405 → 200** (#285/#286/#321, verified by a fresh 652-mutant sweep on `d6c72653`, not by the PR titles). Remaining `causal_arbiter` survivors are concentrated in `calibrate_from_logs` (96) and `arbitrate` (64) and are not equivalent mutants — only 26 of the 200 are log-string-only, 174 change behaviour — but they are unasserted output shape (`p_*` rounding, the fail-open result's zeros), not the anti-poisoning gate, which #321 closed link by link. Open: extending the sweep to a fifth module, and re-running it in CI at all (`ci.yml` carries `# Mutation testing disabled — run locally only`) |
| SR-8 | Doc-vs-code reconciliation | ✅ done — JIT lease is real (`warden/gsam/jit_lease.py`, fail-CLOSED); this table reconciled against code |

## Track B — Deep-Eng / Math (from `docs/modernization-plan-v8.md`)

| ID | Item | Status | PR |
|---|---|---|---|
| DE-1 | GSAM downstream completion | ✅ merged | #150 (+ GSAM chain #138–#143 open) |
| DE-2 | Two-phase preflight billing (reserve→commit) | ✅ merged | #151 |
| DE-3 | Embedding + hyperbolic model upgrade (TDA H1 slice done) | 🟡 in-flight | #152 (slice) |
| DE-4 | Bayesian MAESTRO & reputation (collusion slice done) | 🟡 in-flight | #153 (slice) |
| DE-5 | Causal Arbiter online Robbins-Monro calibration | ✅ merged | (c946b1f1) |
| DE-6 | **Data-layer consolidation** + ClickHouse | ✅ merged — `WARDEN_DATA_DIR`, /tmp sweep, encrypted nightly backup, DDL registry, `GET /gsam/health` (CH was already on in compose) |
| DE-7 | Runtime isolation & key hygiene | ✅ 3/3 complete — fail-CLOSED mandate signing (unsigned-mandate bypass fixed, 17a57067) + agentic boundary gate for SOVA/Master (8e12047e) + BrowserSandbox process isolation. The third item was actually shipped under the §6d self-security backlog as **S4** (2026-07-17, not tracked back to this row until now): `_browser_launch_args()`/`BROWSER_ENABLE_SANDBOX` makes `--no-sandbox` opt-out (was unconditional) + `security_opt` seccomp/AppArmor sidecar wiring on `arq-worker`, plus `warden/agent/tool_budget.py` — every in-process browser-tool dispatch (`visual_assert_page`/`visual_diff`) wrapped in an `asyncio.wait_for` timeout budget + an `asyncio.Semaphore` concurrency cap, fail-SAFE (structured `tool_timeout` error, not a hang/crash). 11 tests (`test_tool_budget.py`) + `test_browser_sandbox_args.py`. Merged + deployed (PR #157 → 6533002d, hotfix #158 → 873cb9a0). This row was stale — the work existed on `main` the whole time under a different initiative name; found via memory cross-check, verified against actual code before closing. | #157, #158 |
| DE-8 | SOVA agent runtime modernization | ✅ core shipped (branch `feat/sova-modernization`) — **P1** prompt-cache the 75-tool prefix + parallel tool dispatch + centralized model (`claude-opus-4-8`, `SOVA_MODEL`); **P2** cost tracking via staff economics (`agent_id="sova"`) + `AgentSpan` structured logs; **P3** activated the previously-dead semantic-memory recall/store (pgvector, fail-open) + cache-safe tool profiles (`full`/`ops`/`community`/`compliance`); **P4** adaptive routing (Sonnet default, Opus on complexity/HIGH MAESTRO; `SOVA_ADAPTIVE_ROUTING`) + `stream_query()` + SSE `POST /agent/sova/stream`. 18 unit tests. Reuses `staff/economics.py` + `staff/structured_log.py` read-only — additive `staff_action_costs` rows, no schema/pricing change (C-shared, see Track C note). | _pending_ |

## Track C — FinOps / Monetization (from `docs/fintech-development-plan.md`)

| ID | Item | Status | PR |
|---|---|---|---|
| FM-0 | Quick wins: SARIF export, `cached_tokens` GSAM column, uptime monitors ×4 hostnames | 🟡 2/3 — SARIF + `cached_tokens` merged (4ab0e15f); uptime monitors folded into FM-5 | 4ab0e15f |
| FM-1 | Unified wallet availability math (prepaid+trial+bonus−hold, one `available_usd()`) | ✅ core shipped — `warden/finops/wallet.py`: `available_usd()` single formula (components floored, holds subtracted, micro-precision), `WalletComponents`, `spend_breakdown()` (free money first: bonus→trial→prepaid), `resolve_wallet()`/`resolve_available_usd()` resilient adapters composing prepaid+hold from `sac.preflight` and trial/bonus grants (Redis `finops:grant:*`, default 0). No schema change to the SAC ledger. 15 tests | _pending_ |
| FM-2 | Real-time cost rating — CH `billing_session_ledger` SummingMergeTree + MV, prompt-cache discount | ✅ core shipped — `warden/finops/rating.py` single price-book (90% cache-read discount, `rate_usage`/`blended_input_rate`); `compute_cost_usd` cache-aware; `billing_session_ledger` SummingMergeTree + MV (units-only, rate at read) applied fail-open in `ensure_schema` + init.sql | _pending_ |
| FM-3 | Margin-aware model routing + per-tier pricing floor (additive after security gates) | ✅ core shipped — `warden/finops/margin.py`: `margin_fraction`, `pricing_floor_usd`, `evaluate_margin` (proceed/throttle/block), `pick_model_within_margin` (most-capable model clearing the floor, never routes below the allowed set), `tier_revenue_per_request` (price÷quota from billing, resilient→None=no floor). Pure + advisory, additive after fail-closed gates. 23 tests | _pending_ |
| FM-4 | 4 GB-node efficiency: mem-limit audit, M/G/1 capacity ceiling; MILP archived until ≥2 nodes | ✅ core shipped — `warden/finops/capacity.py`: Pollaczek–Khinchine M/G/1 (`mg1_wait_seconds`/`mg1_response_seconds`/`utilization`), capacity ceilings (`max_rps_for_utilization` ρ-cap + `max_rps_for_latency` closed-form solve + binding `capacity_ceiling`), `audit_mem_limits` (over-commit vs schedulable RAM) + resilient `parse_compose_mem_limits`. MILP left archived (single node). 17 tests | _pending_ |
| FM-5 | Reliability = revenue: 2× cloudflared replicas, error-budget burn-rate alerts | 🟡 in-flight — pure error-budget + multiwindow burn-rate math (`warden/reliability/`) + `GET /monitors/error-budget` + **Slack burn-rate alert wired** (`sova_error_budget_alert` ARQ cron every 30m: reads per-window uptime from `probe_results`, evaluates the SRE multiwindow table, pages/tickets to Slack with per-(monitor,severity) Redis cooldown; 11 tests) landed; cloudflared replicas + creating the 4 uptime monitors remain (operational, needs prod coord) | — |
| FM-6 | Growth accounting: funnel events, viral coefficient K, LTV/NRR dashboard | ✅ core shipped — `warden/finops/growth.py`: `build_funnel` (per-stage + top-of-funnel conversion, worst-leak detection), `viral_coefficient` (factored K = invites/user × acceptance × activation, verdict self_sustaining/healthy/weak/dead + amplification 1/(1−K)), unit economics (`arpa`, `logo_churn`, `net_revenue_retention`, `ltv`, `ltv_cac_ratio`, `payback_months`, `unit_economics` bundle with SMB healthy gate LTV:CAC≥3 & payback≤6mo), `resolve_referral_k` resilient adapter reading `billing/referral.py` redemption counters. Pure + observational, no storage added. 34 tests | _pending_ |

| FM-7 | Monetization coherence + LLM COGS attribution and control | ✅ shipped — **the three price lists disagreed**: `billing/router.py` served $39.99/$99.99 while a hand-written `feature_gate.ANNUAL_PRICING` (built off the retired $19/$69 list) sold a year of Pro for **$703 against a $1 199.88 list — a 41% discount on a plan advertised at 15%** — and `finops/margin.py` rated revenue off a third private copy. New `warden/billing/pricing.py` is the canonical table (prices, tier aliases, **derived** annual); the other three re-export from it and the alias map is shared with `_normalize_tier` so a tenant can't be priced on one plan and gated on another. **COGS:** MasterAgent (all four call sites: supervisor, sub-agent turns, batch, fallback) and the Evolution Engine — the two most expensive paths — recorded *nothing*; both now write to `TokenCostTracker`, which gained `cached_tokens` + `get_cost_since()` and a `warden_llm_cost_usd_total` counter. `rating.price_for()` gained a model-family fallback because `claude-opus-4-6` was missing from `PRICE_BOOK` and rated as Sonnet (**5× under**). **Control:** `warden/finops/llm_budget.py` derives a per-tier monthly inference allowance (`price × (1 − TARGET_GROSS_MARGIN)`) and picks a model inside it — soft, additive, fail-open, never below `candidates[0]`, wired into MasterAgent; `EVOLUTION_DAILY_BUDGET_USD` ($5) bounds evolution spend (rate-capped ≠ spend-capped: ~$750/day sustained). `GET /billing/margin`, `scripts/finops_report.py`, `docs/monetization-plan.md`. 56 tests incl. a pricing-coherence ratchet. Site + 9 root docs raised to the canonical list | _pending_ |

Track C shared-file notes: `staff/economics.py` pricing dict (C-shared with Track B GSAM), GSAM schema (`gsam/schema.py` + `docker/clickhouse/init.sql` must stay in sync), `sac/preflight.py` wallet, and — since FM-7 — `billing/pricing.py`, which Track F must also treat as the only home for a list price. FM never weakens a security gate — margin logic is additive after fail-closed checks.

---

## Track F — Fintech / Money Layer (from `docs/fintech-grade-commerce-plan.md`)

Registered 2026-07-18. Sequencing lives in `docs/master-kickoff-plan.md`; target
structure in `docs/fintech-architecture.md`. FT owns **money semantics**
(ledger, authorization posture, settlement, financial compliance); ledger
*storage* is C2-shared with Track B (DE-6 `open_db`/`ddl_registry`); cost/margin
*math* stays Track C (`finops/rating.py` price-book is the only rate table).

| ID | Item | Status | PR |
|---|---|---|---|
| FT-3a | AP2 key hotfix — per-call Fernet resolution, no `Fernet.generate_key()` fallback, fail-closed in prod (pulled forward from FT-3) | ✅ done — `_fernet()` per-call, backward-compatible with `VAULT_MASTER_KEY` ciphertext, `InsecureKeyError` in prod; 6 regression tests (`test_ap2_vault_key.py`). Committed on `deeng/de6-p1-open-db-helper` pending clean rebranch/merge | 83177629 |
| FT-0 | Money foundations: `ledger/money.py` (int micro-USD), money-mutation inventory, float-money `REAL`-column ratchet | 🟡 core done — `warden/ledger/money.py` (`Money` int µUSD, Decimal-only boundary, float rejected, conservation-safe `split_fee`), 26 tests; `docs/money-mutation-inventory.md`; `test_no_new_real_money_columns.py` ratchet (baseline 51, may-only-drop). Float-arithmetic ratchet deferred (needs low-false-positive money-path definition) | — |
| FT-1 | Double-entry ledger core (`warden/ledger/` journal/accounts/holds/rollup, hash-chained, idempotency-keyed) | 🟡 core done — `accounts.py` (closed `ns:owner:leaf` grammar + canonical constructors + normal-side) and `journal.py` (`post()` balanced Σ=0 + `Idempotency-Key` UNIQUE replay-no-op, immutable rows, SHA-256 per-tx chain + `verify_chain`, `balance()` derived from postings, `open_db`+DDL-registry `ledger` key, integer µUSD). 16 tests incl. conservation over 300 txs + tamper detection. ✅ `holds.py` (two-phase reserve→capture/void, journal-backed, idempotent per phase) + `rollup.py` (materialized `ledger_balances` cache, journal-derived `refresh`/`balance`) done — 17 more tests. FT-1 complete; feeds FT-2 | — |
| FT-2 | Migrate balance writers (credits, wallet funding, preflight holds) + trial/bonus as `promo:*` accounts (NF-1) + monetary referral kickback (NF-2) | 🟡 foundation done — `warden/ledger/operations.py`: canonical idempotent journal-backed flows (`topup`, `grant_trial`, `grant_bonus`, `grant_credits`, `purchase` w/ conserving fee-split, + `reserve`/`capture`/`void` re-export) = the target API. 11 tests. ✅ slice 2a: `dual_write.py` (dual-run bridge — `LEDGER_DUAL_WRITE` gate + fail-OPEN `mirror()` + generic `reconcile()`) + `spend_credits` op + `settings.ledger_dual_write` flag; cutover = **dual-run + reconcile** (chosen). ✅ slice 2b: `marketplace/credits.py` grant/deduct now mirror into the ledger (guarded, `record_failopen`-counted, default-off) — first live writer wired; 8 wiring tests + also fixed a latent counterless-failopen ratchet miss from 2a's `mirror()`. ✅ slice 2c: `sac/preflight.py` reserve/commit/release now mirror `operations.reserve`/`capture`/`void` (guarded, `record_failopen`-counted, gated default-OFF; commit capped at held). 7 wiring tests. ✅ slice 2d: `finops/ledger_recon.py::credit_drift()` — the reconciliation keystone; enumerates every tenant's credit balance (`credits.all_balances()`, new) and compares to the ledger via `dual_write.reconcile()`, fail-soft (never blocks, never raises). 5 tests incl. zero-drift-after-mirrored-grants and drift-detection-on-unmirrored-write. Trial/bonus grant writers deferred — no clean single write path exists yet (only a read in `finops/wallet.py`). FM-1 `available_usd()` re-point (the read-cutover) stays deferred until an operator runs `LEDGER_DUAL_WRITE=true` in prod and `credit_drift()` shows zero over a shadow period — genuinely a human go/no-go, not a code slice. **Shadow period opened 2026-08-12**: the flag is live in both `warden` and `arq-worker` (`settings.ledger_dual_write = True`, gateway healthy). ⚠️ It could not have been opened before #320 — `LEDGER_DUAL_WRITE` was missing from `docker-compose.yml`, so setting it in `.env` had never reached the container and any earlier "bake" would have measured a mirror that was never running. ⚠️ **Do not read the clock as the gate.** The first reconciliation reads `{tenants_checked: 0, drifted: 0, ok: true}` — `ok` by vacuity, not by agreement: production currently holds zero credit rows, so "reconciled" and "nothing to reconcile" are indistinguishable, which is the exact failure shape the F8/journal work spent this cycle removing elsewhere. The read-cutover needs `credit_drift()` clean with **`tenants_checked > 0`**, not a date. ✅ **evidence slice (2026-08-14)**: that gate was *forgeable*, and was measured to be so against production — `tenants_checked` counted the tenants **enumerated**, while a tenant whose `dual_write.reconcile()` raised was silently `continue`d and still counted, so a ledger unreachable for **every** tenant reported `{tenants_checked: 2, drifted: 0, ok: true}` — the documented go/no-go satisfied with nothing verified. Both reports now carry an `evidence` label (`counted` / `nothing_to_check` / `not_available`), count only genuinely reconciled subjects in `*_checked`, report `unreadable` separately, and `record_failopen()` every swallowed read error onto `warden_stage_failopen_total{stage=ledger_recon_*}`. `read_cutover_ready()` encodes the gate as code (positive evidence required; open holds may legitimately be empty, credits may not) and is runnable as `python scripts/finops_report.py --ledger-gate` (exit 0 = ready, 2 = not). The nightly job now alerts on a run that verified nothing **because a read failed** (`evidence=not_available`, or any `unreadable` subject) — previously logged as "clean". `evidence=nothing_to_check` stays silent by design: an empty credits table is a normal quiet night, and paging on it nightly would train everyone to ignore the alert. Production reads `evidence=nothing_to_check` — the shadow period has still observed zero credit rows since 2026-08-12, which is now stated rather than reported as agreement | — |
| FT-3 | Idempotency-Key on money endpoints + remove financial `INSERT OR REPLACE`. ⚠️ Scope note: the original plan's "fail-closed money-authorization posture flip" (x402/autonomy exception→deny) is **rejected** — contradicts standing invariants (root `CLAUDE.md` "x402 fail-open", `marketplace/CLAUDE.md` #13/#20); confirmed with the project owner 2026-07-20. Remaining FT-3 scope is idempotency-only | ✅ complete — **3a** `marketplace/clearing.py`: `clearing_id` now deterministic (`clear-{winner_neg_id}`, was `uuid.uuid4()` — every retry minted a fresh row); `INSERT OR REPLACE` → `ON CONFLICT DO NOTHING` + read-back; `clear()` returns the existing row on replay instead of re-running auto-reject + fee computation. Consolidated 5 raw `sqlite3.connect` sites → 2. 6 tests. **1b** (#175) `marketplace/api_escrow.py`: transition endpoints now 404 on nonexistent escrow vs 409 on illegal state transition (was undifferentiated 400). **3b** `marketplace/credits.py` + API: `POST /credits/purchase` had **zero** idempotency key — a retried Lemon webhook/double-submit granted credits **twice** (real duplicated spendable balance, not just a log row). `purchase_credits(..., idempotency_key=...)` now records each grant (`marketplace_credit_purchases` table) and returns the cached balance on replay; whole check→grant→record sequence serialized under the module's existing `_db_lock` to close the concurrent-race window; endpoint requires an `Idempotency-Key` header (400 if missing). 7 tests incl. an HTTP-level double-POST-same-key test. **3c** `marketplace/listing.py` + API: `purchase_listing()` (the `buy`/`POST /listings/{id}/purchase` path) had **zero** idempotency key either — a retried call created a SECOND purchase row AND a second escrow for the same buyer intent, a real double-charge. Added a nullable `idempotency_key` column + partial-unique index (`WHERE idempotency_key IS NOT NULL`, so legacy/no-key rows stay unconstrained); check-then-create sequence serialized under the existing `_db_lock`; replay returns the original response (incl. real `escrow_id`, now persisted back onto the purchase row post-creation — previously always `""`); endpoint requires `Idempotency-Key`. 7 tests. Updated 2 pre-existing integration tests (`test_hub_marketplace_flow.py`, `test_production_readiness.py`) that called the now-gated endpoint with no header. **FT-3 idempotency scope is now complete** (clearing, credits, listings — the three money-moving endpoints) | #174, #175 |
| FT-4 | Settlement worker + nightly reconciliation + transactional outbox (absorbs NF-3) | ✅ complete — slice 1 done — `warden/workers/x402_settlement.py::settle_pending_deductions()`: drains `x402_pending_deductions` (`status='pending'` rows, previously accumulated forever with zero settlement) — marks each row `settled` + `settled_at`, gated per-row on `status='pending'` so it's idempotent under a re-run or a concurrent race (lost race ⇒ 0 rows touched, not an error). Returns a per-run `SettlementSummary` (settled count, distinct agents, total USD) for audit/reconciliation. Fail-soft: one bad row is skipped and counted in `errors`, the rest of the batch still settles; a missing DB/table (no x402 activity yet) returns an empty summary rather than raising. Wired as `settle_x402_deductions` ARQ job, cron every 15 min in `warden/workers/settings.py`. Deliberately does not (re)declare the table's own DDL to avoid a duplicate `REAL` money-column ratchet hit — relies on x402_gate.py's existing table, additive `ALTER TABLE` only for the new `settled_at` column. 6 tests; 242 marketplace+x402 tests green; ruff+mypy clean; 4 ratchets green. ✅ slice 2 done — `warden/workers/ledger_recon_job.py::run_ledger_reconciliation()`: wires the pure `finops/ledger_recon.py::credit_drift()` (which existed since FT-2 slice 2d but was never scheduled or observable — its own docstring said "that is FT-4") into the `warden_ledger_recon_drift_usd` Prometheus gauge + a Slack alert on nonzero drift; ok state fires no alert. Wrapper only — `credit_drift()`'s own fail-soft posture (never raises, ok-by-vacuity on read error) is untouched. Cron `nightly_ledger_recon`, daily 04:00 UTC. 10 tests (5 new + 5 pre-existing pure-function tests re-verified); 239 marketplace/x402/recon tests green; ruff+mypy clean; 4 ratchets green. ✅ slice 3 done — `warden/marketplace/clearing.py`: replaced the fire-and-forget async `_write_postgres()` with a transactional outbox. Every `clear_async()` first durably enqueues a `marketplace_clearing_outbox` row (`status='pending'`, idempotent on `clearing_id` — a replay enqueues nothing new), *then* attempts the relay; a failure leaves the row pending (attempts incremented) instead of losing it silently. New `relay_pending()` drains pending rows — wired as `relay_clearing_outbox` ARQ cron, every 5 min. Already-relayed rows are a no-op (no duplicate Postgres write). Full recon (comparing outbox state to what Postgres actually holds) deferred — this slice guarantees nothing diverges *without at least being recorded as owing a retry*. 9 new tests (7 outbox behavior + 2 ARQ wrapper); 238 marketplace/outbox/idempotency tests green; ruff+mypy clean; 4 ratchets green. **Holds reconciliation deferred** — extending the `credit_drift()` pattern to `sac/preflight.py` holds needs a semantics decision (what counts as drift for a hold created before dual-write was enabled: expected absence vs real drift) that wasn't resolved in this pass; asked the user, they chose to prioritize the outbox instead. ✅ outbox retention/cleanup done — `warden/marketplace/clearing.py::purge_relayed_outbox()`: deletes `status='relayed'` outbox rows older than `older_than_days` (default 30); `status='pending'` rows are *never* purged regardless of age, so a stalled relay can't lose its retry record. Fail-soft (read/delete error returns a zero summary, never raises). Wired as `purge_clearing_outbox` ARQ cron, weekly Sunday 05:00 UTC, in `warden/workers/clearing_outbox_relay.py`. 4 new direct unit tests (pending-never-purged, within-window kept, past-window purged, fresh-db noop) + 2 pre-existing ARQ-wrapper tests; 15 outbox tests green; ruff+mypy clean; 4 ratchets green. ✅ holds reconciliation done — resolved the deferred semantics decision: `sac/preflight.py::open_holds()` enumerates every currently-open (`status='HELD'`) hold; `finops/ledger_recon.py::hold_drift()` reconciles each individually against its own `hold:{hold_id}` ledger contra account, since a hold is a per-transaction account (not a per-tenant running balance like credits). A hold that predates dual-write has no mirrored ledger entry and would misreport as drift — but holds are short-lived (reserve → commit/release within one agent run), so any such false positive self-clears the moment the hold resolves and drops out of `open_holds()`, unlike an unbackfilled credit balance that would drift indefinitely. Wired as `nightly_hold_recon` ARQ cron (daily 04:10 UTC) + `warden_ledger_recon_hold_drift_usd` gauge + Slack alert, same wrapper pattern as slice 2. 17 new tests (5 `open_holds()` + 7 `hold_drift()` + 5 job wrapper); ruff+mypy clean; 7 ratchets green. Remaining, deliberately out of scope: full recon comparing outbox status to actual Postgres state (slice 3's at-least-once posture is an accepted permanent tradeoff, not a gap) | — |
| FT-5 | Compliance: KYB behind KYA, sanctions at settlement, AML on journal stream, licensing-posture doc | 🟢 4/4 DONE — `docs/licensing-posture.md` done (see prior status). ✅ KYB behind KYA done — `warden/marketplace/kyb.py`: owner (tenant)-level verification sitting behind agent-level KYA, `KYBRecord`{tenant_id, kyb_status PENDING/VERIFIED/FLAGGED/REJECTED, business_name, reviewer, notes}, manual-review queue (`submit_for_review`/`approve_kyb`/`reject_kyb`/`flag_kyb`) plus a pluggable `KYBProvider` interface (only `ManualReviewProvider` implemented — always defers to a human, v2 slot for Persona/Sumsub). `warden/marketplace/autonomy.py::check_action()` now caps an agent at REQUIRE_APPROVAL when its KYA-registered owner isn't KYB-VERIFIED — "agent inherits the owner's status" — gated behind `KYB_ENFORCEMENT_ENABLED` (default **false**, matching every other compliance gate in this codebase: `ledger_dual_write`, `KYA_VERIFIED_ONLY`) so existing tenants are not retroactively capped the moment this ships; a lookup failure while enforcement is *on* fails conservative (capped), but if enforcement is off or even unreadable it always resolves to unchanged behavior — verified with a dedicated test forcing `enforcement_enabled()` itself to raise. No "payout hold" half implemented — per the licensing-posture doc's own finding, there is no real payout mechanism anywhere in the codebase to hold. 21 new tests (15 kyb.py + 6 autonomy integration); 116 marketplace/kya/autonomy tests green; ruff+mypy clean; 4 ratchets green. ✅ Sanctions screening at settlement done — `warden/marketplace/sanctions.py::screen_settlement_party()` reuses the existing STAFF-05 `screen_sanctions_list()` tool against the buyer of every `clearing.py::clear_async()` run; resolves the buyer's owner tenant via KYA and its display name via KYB's `business_name` when available. Additive/observational only — a HIT never blocks or delays the transaction (the builtin denylist is a stub, not a real OFAC/EU feed, so an automatic hard block isn't this code's call to make); instead opens a `COMPLIANCE`-category incident via the already-STIX-linked `incident_register.log_incident()`. Opt-in via `SANCTIONS_SCREENING_ENABLED` (default false, same pattern as `KYB_ENFORCEMENT_ENABLED`). Scoped to the buyer only — seller screening needs a listing→seller_agent_id resolution `ClearingResult` doesn't carry yet, left as explicit follow-on rather than added here. 23 new tests (10 sanctions.py + 2 clear_async wiring, plus reused KYA/KYB fixtures); ruff+mypy clean; 4 ratchets green (removed 4 dead `# noqa: PLC0415` comments along the way — that rule isn't in this repo's selected ruff set, so they were pure suppression-ratchet noise). ✅ AML on the journal stream done — `warden/finops/aml_monitor.py::scan_for_structuring()` batch-sweeps every `tenant:*:cash` account (new `journal.distinct_accounts()`/`postings_for_account()` read primitives) for structuring/smurfing: postings individually below `AML_STRUCTURING_THRESHOLD_USD` (default $10k) whose rolling-window (`AML_STRUCTURING_WINDOW_HRS`, default 24h) sum reaches the threshold across at least `AML_STRUCTURING_MIN_POSTINGS` (default 3) postings — a single large legit transfer never counts. Deliberately a scheduled pull-based sweep (`warden/workers/aml_monitor_job.py`, ARQ cron `nightly_aml_scan` daily 04:20 UTC), not an inline hook on `journal.post()` — the journal is a leaf module with no upward imports of compliance code. Same posture as sanctions/KYB: opt-in via `AML_MONITOR_ENABLED` (default false), fail-soft on any read error (`{"scanned": False, "error": ...}`), a hit only opens a `COMPLIANCE` incident via `incident_register.log_incident()` — never blocks. 40 new tests (aml_monitor.py + job wrapper + journal primitives); ruff+mypy clean; 4 ratchets green. FT-5 fully closed | — |
| FT-6 | Consolidation: single `authorize_payment()` chokepoint (+ ratchet), one x402 impl, one order model (absorbs NF-6/MI-4) | 🟡 2/3 — ✅ `authorize_payment()` chokepoint (`warden/payments/authorize.py`, mirrors `agentic_gate()`) wired into `purchase_listing()` and `clearing.py::clear()`; `credits.py::purchase_credits()` deliberately excluded (Lemon Squeezy webhook fulfillment, not an agent spend decision — see prior entries). ✅ x402 balance-core consolidation done: `warden/payments/x402_balance.py` is now the single owner of the `x402_balances` table's DDL fragment (`X402_BALANCES_DDL`) and its three SQL operations (`get_balance`, `credit_balance`, `deduct_strict`, `deduct_floor`), taking an already-open `sqlite3.Connection` so each caller keeps composing its own extra writes (voice's `x402_transactions` insert, marketplace's `x402_pending_deductions` insert) in the same transaction exactly as before. `voice/x402.py` and `marketplace/x402_gate.py` keep their own physical DB files and `db_key`s (DDL registry requires per-physical-file keys — this was never a "merge into one wallet" job) and their own distinct flows (payment channels + on-chain verification vs. HTTP-gate + replay-nonce + autonomy composition) untouched; only the previously-duplicated balance SQL moved. Two deduct semantics preserved exactly as-is, not unified: `deduct_strict` (voice — rejects, no mutation, if insufficient) vs. `deduct_floor` (marketplace — always subtracts clamped at 0, since `require_payment()` already gated sufficiency earlier). 58 tests total (48 pre-existing x402/mcp-gateway/voice regression unchanged + 10 new for the shared primitive); ruff+mypy clean; 4 ratchets green. 🟡 **Order-model consolidation, in progress**: full-codebase survey found `m2m_orders` and `commerce_orders` are schema-less JSON blobs (`id, data_json, created_at`) while `marketplace_purchases` (the documented target) is fully fielded — genuinely different shapes, no existing column mapping, money-bearing tables read by 6+ consumers (analytics/Sybil/trust-graph/metrics/admin dashboard). Rather than migrate live data blind, wrote `docs/order-model-consolidation-plan.md` (target schema extending `marketplace_purchases` with nullable `domain`/`tenant_id`/`mandate_id`/etc. columns, a column-mapping table, and a 4-phase dual-write→cutover→freeze sequence — mirroring this same plan's own "don't migrate in place, freeze old tables read-only" rule) — mirrors the FT-5 licensing-posture precedent (PR #184) of shipping a design doc first when real implementation isn't safely scoped yet. Landed alongside it: the literal duplicate `commerce_orders` DDL between `ap2.py` and `service.py` (confirmed byte-identical columns, just two different index names) deduped into one `COMMERCE_ORDERS_DDL` constant owned by `ap2.py` — pure DDL-text consolidation, zero data risk, unrelated to the cross-domain migration itself. 57 regression tests pass unchanged; ruff+mypy clean; 4 ratchets green. ✅ **Phase A shipped**: `_migrate_order_consolidation_columns()` in `listing.py` adds the nine nullable columns + `idx_mp_domain`/`idx_mp_tenant` indexes from the plan doc, `domain` defaulting to `'marketplace'` for every existing row — purely additive `ALTER TABLE ADD COLUMN`, zero behavior change, 5 dedicated tests + full marketplace/analytics/Sybil/trust-graph regression suite green. The `commerce_orders`→`asset_id` open question is resolved (relax to nullable) but its constraint change needs a SQLite table rebuild (no `ALTER COLUMN`) — deliberately deferred to Phase B rather than bundled into Phase A's otherwise trivially-safe additive slice. ✅ **Phase B shipped**: `_migrate_relax_asset_id_nullable()` rebuilds `marketplace_purchases` (create-copy-drop-rename — SQLite has no `ALTER COLUMN`) the first time it sees `asset_id NOT NULL`, idempotent via `PRAGMA table_info` check; new `upsert_mirrored_order()` is the single dual-write path, called by `m2m_store.InventoryManager.save_order()` and `agentic_commerce.service._save_order()`/`ap2.py`'s receipt write after their own blob-table write already succeeds. Caught two real bugs before shipping: an `ON CONFLICT` update was overwriting `price_paid` with `0.0` on every status-only follow-up call (fixed by dropping it from `UPDATE SET`, `COALESCE`-guarding the other nullable fields); an explicit `BEGIN IMMEDIATE` collided with a transaction `open_db()` already had open (fixed by removing the explicit transaction and relying on `open_db()`'s own commit-once/close-on-exception). Reads are still from the original blob tables — Phase C's job. 11 new tests + 137 total regression tests (marketplace/analytics/Sybil/trust-graph/agentic_commerce/m2m_store, 12 files) pass unchanged; ruff+mypy clean; 4 ratchets green. **Remaining for FT-6:** Phase C (cutover reads) and Phase D (freeze old tables) of the plan doc. ✅ **Phase C gate built (2026-08-14)**: "needs a dual-write bake period first" was a judgement call with nothing behind it — no reconciliation compared the mirror to the source of truth, and all three dual-write sites swallowed failure into `log.debug` with **no counter**, so a mirror that never wrote once looked exactly like one that worked. `warden/finops/order_recon.py::order_mirror_drift()` now compares every `m2m_orders` / `commerce_orders` row to its `marketplace_purchases` mirror and counts the three distinct failures separately (**missing** = dropped dual write, **mismatched** = status/amount disagreement, **orphaned** = mirror row with no source), with one encoded exception: `ap2.py` upserts `status=PAID` onto a receipted order on purpose, so that divergence is correct and is not reported as drift. Same `evidence` vocabulary as FT-2 (`counted`/`nothing_to_check`/`not_available`, now canonical in `warden/observability.py`); `phase_c_ready()` is the gate as code, runnable as `python scripts/finops_report.py --order-gate` (exit 0 = ready, 2 = not). Wired as `nightly_order_mirror_recon` ARQ cron (04:20 UTC) with a Slack alert on disagreement *or* on a check that compared nothing; the three mirror sites now `record_failopen()`. 15 tests. **Production reads `nothing_to_check`** — both order tables have been empty since Phase B shipped, so like the FT-2 shadow period this gate cannot be satisfied by waiting; it needs real order traffic. | — |
| FT-7 | Assurance: money-conservation property tests, chaos tests, auditor export, SOC 2 mapping | ⬜ continuous | — |

Related non-FT items scheduled in the kickoff plan: MI-1/MI-2 (GSAM taps +
quarantine gate in marketplace — Track B), NF-5 mediator (Track B, DE-4),
MI-5 tunnel enforcement v1 (Track B, coordinate `net_guard` with Track A).

---

## Track P — Platform Hardening (from `docs/deep-analysis-2026-07-27.md`)

Registered 2026-07-27 after a whole-repo assessment scored the project 68/100 —
security engineering ~85, software industrialization ~50. Track P owns the
industrialization half: build determinism, packaging, the configuration seam,
code shape, documentation coherence and the debt burn-down schedule.

**Track P changes shape, never semantics.** Where it touches a file owned by
another track it must run that track's tests and reference it in the PR. It may
not weaken a fail-closed gate to simplify a refactor — `agentic_gate()`,
`resolve_key()`, STAFF-01/02 and the x402 fail-open posture are out of scope by
construction.

| ID | Item | Status | PR |
|---|---|---|---|
| P-0 | Deterministic builds — image installs via the lockfile | ✅ done — `warden/Dockerfile` now installs `-r requirements.txt -c constraints.txt` (it previously did not, while CI always did: a 2026-07-27 measurement found **48** packages differing between the CI-tested set and the running production image, one of which — `prometheus-fastapi-instrumentator` 8.0.2→8.1.0 — caused the 2026-07-26 full-API outage). torch/liboqs pinned explicitly (outside PyPI resolution by design); `constraints.txt` gained `clickhouse-connect`/`setuptools`/`wheel`; `pip-audit` now audits the locked set instead of newest-of-everything; `analytics/Dockerfile`'s unquoted `pip install fastapi>=…` shell-redirect bug fixed; orphaned `warden/requirements-lock.txt` deleted (closes conflict C-B). Ratchet `warden/tests/test_deps_pinned.py` (9 hermetic tests, verified to fail when `-c` is removed). Dependabot triage still open | — |
| P-1 | Single version of record (`pyproject` 5.3.0 vs docs 7.7 vs code 7.8) | ⬜ next | — |
| P-2 | Finish dissolving `main.py` (3,680 lines, 23 inline routes) | ⬜ | — |
| P-3a | Multi-stage build — no compiler in the runtime image; **build liboqs for real** | ✅ done — runtime apt layer 466 MB → 116 MB (gcc/g++/cmake/ninja-build/astyle/libssl-dev gone from the running gateway). Uncovered and fixed a bigger problem on the way: **PQC had never worked in any deployed image** since v4.7 — `pip install liboqs-python` installs bindings only, the native CMake build was never run, the step was `\|\| echo` fail-open, and `pqc.py` logged the RuntimeError as "not installed" so `pip freeze` appeared to contradict it. Verified against prod: no `liboqs.so`, `pqc_status() → available: False`, and `hybrid_verify()` silently accepting Ed25519-only. Builder stage now compiles liboqs 0.16.0; the image **fails to build** unless ML-DSA-65 + ML-KEM-768 load; `pqc_selfcheck()` wired into lifespan (ERROR) and `GET /health/pipeline` (`pqc` key, deliberately not in `degraded_stages`). Verified roundtrip in the built image: 1952-byte ML-DSA-65 pubkey, 3373-byte hybrid signature, tamper rejected. Also caught a self-inflicted regression before shipping — `pip install --prefix=/install` hid the CPU torch from the later resolve, pulling **CUDA** torch (+2.7 GB nvidia-*, +691 MB triton); fixed, with a build-time assertion + 2 ratchet tests | — |
| P-3b | Keep the ML stack out of the 1 GB ARQ worker | ✅ done — **the original framing of this item was backwards.** It was filed as "split the gateway from the tools image, only the worker needs torch/whisper/opencv". Measured: the opposite. The *gateway* is the 8 GB / 4 CPU service that legitimately loads MiniLM; `arq-worker` is capped at **1 GB / 0.5 CPU** and delegates all inference to `warden:8001` over HTTP. Yet it was importing torch + sentence-transformers + transformers + scipy + scikit-learn at boot — ~500 MB, ~40% of its budget, for models it never runs. Root cause was not the shared image at all: `warden/brain/__init__.py` eagerly re-exported `SemanticGuard` from `semantic.py` (which imports torch at module level), so importing **any** module in the package — including the torch-free `online_learner`, which is all the worker wants — dragged in the whole stack. Fixed with a PEP 562 module `__getattr__`: `import warden.brain.online_learner` went from 3 659 to 225 `sys.modules`, and the worker's full `WorkerSettings` import now loads **zero** ML libraries while still registering all 29 jobs. The nightly `online_learning_job` is unaffected — `online_learner` already imported `semantic` inside its functions, so it loads torch at job time instead of worker boot. Gateway is untouched (`main.py` imports the submodule directly and still pre-warms in lifespan). Ratchet `warden/tests/test_brain_lazy_import.py`, incl. a guard against "fixing" this by making `semantic.py` itself lazy, which would move a multi-second import onto the first `/filter` request | — |
| P-3b′ | Split gateway from tools image (`warden` vs `arq-worker`) | ⬜ — now a much smaller prize than P-3b assumed. The runtime *weight* problem is solved in-process; what remains is image size on disk and blast radius (the worker still ships torch it can now avoid importing). Re-measure before committing to a second Dockerfile | — |
| P-4 | Close the configuration seam (545 `os.getenv` outside `Settings`) | ⬜ | — |
| P-5 | Debt burn-down: make the frozen baselines decrement | ⬜ continuous | — |
| P-6 | Deadlines for the half-migrations (FT-6 Phase C/D, FT-2 read-cutover, 5 default-OFF gates) | ⬜ needs Track F | — |
| P-7 | Documentation consolidation (71 files, 5 duplicate basenames) | ⬜ | — |
| P-8 | API-surface reduction (769 routes / 108 modules) | ⬜ | — |
| P-9 | Capacity truth (20.4 GB of declared limits, unverified) + HA decision | ⬜ | — |
| P-10 | CI workflow decomposition (58 KB `ci.yml`, 18 jobs) | ⬜ | — |

Track P shared-file notes: `warden/Dockerfile` and `warden/constraints.txt` are
supply-chain surfaces co-owned with Track A (SR-7); `warden/config.py` (P-4) is
read by every track; P-6 sets deadlines but Track F owns the money semantics
behind them.

---

## Conflict zones (shared files — ownership rules)

| # | File / surface | Track A touched | Track B touched | Reconciliation rule |
|---|---|---|---|---|
| C1 | `warden/causal_arbiter.py` | SR-3: zero-prior drift-gate hole fix + 25% CPT gate | DE-5: online Robbins-Monro calibration | **DE owns the math.** Any calibration change MUST keep the 25%/zero-prior drift gate (anti-poisoning). Add a test asserting both hold together. |
| C2 | Data layer (62 `_conn`/DDL copies, Turso/SQLite/ClickHouse) | SR-5 (unify connection layer) | DE-6 (ClickHouse on, category 11) | **Single effort.** Merge SR-5 + DE-6 → one `warden/db/` consolidation, owned by Track B (they own storage). Track A supplies the "one context-manager, DDL-once" requirement. |
| C3 | `staff_dispatch` / `BoundaryRegistry` | SR-4.2: A2A boundary+suspension enforcement | GSAM quarantine additive gate; DE-7/S5: extend to SOVA/Master | **Additive gates only**, after boundary check; never weaken STAFF-01/02. Track B's S5 extension builds on SR-4.2 — coordinate order. |
| C4 | SSRF (`net_guard`, Inner Warden) | SR-2: 8 sinks guarded | SAC "Inner Warden SSRF fail-CLOSED" (#150) | Deduplicate: one guard path. SR-2.3 IP-pinning, if done, lands in `net_guard` and both consume it. |
| C5 | Key hygiene (`resolve_key`, JIT lease) | SR-4.4 refund key via resolve_key | DE-7 key hygiene; SAC/GSAM JIT lease (now real) | SR-8 doc-reconciliation marks JIT lease SHIPPED (it exists now) and closes the old doc-vs-code gap. |
| C6 | Marketplace order/money surfaces (`listing.py`, `clearing.py`, `mp_*` models) | — | Track F owns order model + `authorize_payment` semantics | **Track F owns money semantics.** MP-5 (dead `mp_*` projection) and MP-6 (enforcement default) are consumers and need F sign-off — MP never adds an order-shaped writer while FT-6 is reducing their count. MP-1 (authn + signature verification) is request-path integrity and is **not** F-gated. |
| C7 | Marketplace router auth | SR/Track A owns a global auth middleware (the `/communities` root cause, PR #239) | — | MP-1a is the marketplace-local fix (`Depends(require_api_key)` per router). If Track A lands a global middleware, it subsumes MP-1a; MP-1b (offer-signature actor proof) is independent and still required — an API key proves a tenant, not which agent signed. |

---

## Owners (assigned 2026-07-12)

- **Track A — Security Remediation** owns: authn/authz, SSRF wiring, IDOR/GDPR, request-path invariants, CI/supply-chain hardening (SR-2.4, SR-6, SR-7, SR-8, SR-1.4b).
- **Track B — Deep-Eng / Math** owns: all ML/detection math (TDA, MAESTRO, Causal, embeddings), GSAM, storage/data-layer (incl. the merged SR-5+DE-6), runtime-isolation math.
- **Shared, coordinate before touching:** the five files in the conflict table. Rule: whoever edits one references the other track in the PR description and runs the other track's relevant tests.

## Decisions log

- **2026-08-01 — Track M registered (`MP-*`), source `docs/marketplace-modernization-plan.md`.** A structural audit of the M2M marketplace cluster (12.5k LOC, 64 routes, 410 tests green) found **four of the 22 "non-negotiable" rules in `warden/marketplace/CLAUDE.md` describe code that does not exist**: offer Ed25519 signing (`_verify_offer_signature()` is defined and called from nowhere; all API offers persist `signature=''`), the injection guard (`injection_guard.py` is dead — a weaker private substring dupe runs instead), escrow-on-`accept_offer()` (zero escrow references in `negotiation.py`), and the `mp_*` Semantic Layer projection (projector has no production caller ⇒ 5 models over permanently-empty tables). Combined with **no auth on the negotiation router** (`marketplace_rate_limit` is a rate limiter; no global auth middleware in `main.py` — same root cause as the `/communities` split-brain, PR #239), this was demonstrated end-to-end: unauthenticated caller settles a $1000 listing at $0.01 by impersonating the seller on accept. Track M owns marketplace **request-path integrity**; money semantics stay with Track F (new conflict rows C6/C7). Critical path is MP-0 (doc truth) → MP-1 (authn + signature verification); MP-1 must not be batched with other slices. Explicitly **not** in scope: flipping x402/autonomy fail-open, rejected by the owner 2026-07-20 (FT-3).
- **2026-08-01 — Track M route-auth inventory: 30 of 55 cluster write routes have no auth dependency.** Measured, not inferred. It is **drift, not design** — `/marketplace/{escrow,listings,clear,action,autonomy}/*`, all `/m2m-store/*` and all `/a2a/*` do carry `require_api_key`, while neighbouring routes *in the same files* do not: negotiations (3), agent identity incl. **rotate-key** (5), DAO proposals (3), assets/register/certificates/credits (4), `/acp/*` (7), `/business-community/commerce/*` (7, incl. `webhooks/ap2` with no dependency at all). Two "looks-authenticated-but-isn't" patterns named for the first time and added to `marketplace/CLAUDE.md` under MP-0: **(i) a feature gate is not auth** — `/acp/*`'s `_Gate` is `billing/feature_gate.py::require_feature`, which resolves a tier and 403s if too low but never establishes identity (⚠️ **corrected during MP-8 execution**: these routes are *not* anonymously reachable — measured 403 on `origin/main`, and `X-Tenant-Tier` cannot be spoofed since SR-1.1. The real defect is narrower: `tenant_id` is caller-supplied body text, so any caller whose plan clears the gate can act against **another tenant**, and access control rests on a side effect of tier resolution); **(ii) an empty secret must not disable a check** — `api.py:1237` `if admin_key and ...` means `POST /marketplace/agents/{id}/kya/revoke` skips its admin check entirely when `ADMIN_KEY` is unset (MP-1c, same shape as the FT-3a key hotfix). ⚠️ **Enumeration caveat:** under starlette ≥ 1.0 `include_router()` leaves a `fastapi.routing._IncludedRouter` placeholder, so a naive `app.routes` walk sees 129 routes while OpenAPI reports 657 — any route audit must use `test_route_inventory.py`'s `.original_router`/`.include_context.prefix` walk or it undercounts by ~80%.
- **2026-07-18 — S7 done; §6d S1–S7 security-hardening backlog CLOSED.** S5 was recognised as already delivered (= DE-7 Phase 7 Slice 2, `agentic_gate()`, 2026-07-13). S7 repaired the weekly DAST (`pentest.yml`): Nuclei install 404 fixed (versioned release asset via API), and a new `zap-api-scan` job drives ZAP off `/openapi.json` against an ephemeral CI-booted warden — the old "green" baseline had scanned exactly one URI (`/robots.txt`). Pen-test (annual) + threat-model refresh (semi-annual) cadence in `docs/security-model.md` §8.
- **2026-07-12 — Registry adopted as canon.** Wired into `CLAUDE.md` ("Modernization Governance") so both efforts read the same rules. `SR-*`/`DE-*` prefixes are now required; bare "Phase N" is retired.
- **2026-07-12 — Track owners assigned** (above). The session that has been landing security PRs (#148/#149/#154) drives **Track A**; the session landing the deep-eng/GSAM PRs (#150–#153, Phase-6 data-layer) drives **Track B**.
- **2026-07-12 — C2 (data-layer) RESOLVED: Track B leads.** SR-5 does **not** spawn a separate DB-consolidation effort; it folds into DE-6, which is already in flight (`421f2ea6` "data-layer consolidation, Phase 6 slice 1"). Track A's contribution to DE-6 is a requirement, not a parallel PR: *one connection context-manager, DDL applied once at startup (not per call), guaranteed `close()`; retire the ~62 duplicated `_conn`/DDL helpers.* Track A will review DE-6 PRs against that checklist.
- **C1 guardrail ✅ (2026-07-14):** `warden/tests/test_causal_c1_guardrail.py` asserts DE-5 online calibration and the SR-3 25%/zero-prior drift gate coexist — shared `0.25` bound, per-step online clamp, batch gate still rejects >25% drift after online updates, ordering invariant survives adversarial slow-burn, zero-prior safe on both paths.

- **2026-07-13 — SR-8 reconciliation done; Track A closed out.** The table above was re-derived from
  the code, not from the plan text. Findings worth recording:
  - The plan's "JIT lease missing" gap (SR-8.1) is **stale** — `warden/gsam/jit_lease.py` exists and is
    fail-CLOSED. Likewise SR-4.4's refund key was already on `resolve_key`.
  - SR-5 was never worked as a separate effort (C2 held): it is satisfied by DE-6.
  - SR-6 was already satisfied by the FAILOPEN-01 ratchet + `warden_stage_failopen_total`.
  - Enabling bandit surfaced **three real HIGH findings the plan had not listed**, now fixed:
    `verify=False` on the MISP threat-intel feed (a MITM could inject IOCs/rules straight into the
    detection corpus) and on the LND client (which ships a bearer macaroon); SHA-1 in a cache key; and
    raw bidi/trojan-source control characters in `obfuscation.py`'s own source (now codepoints).
    `shadow_ai/discovery.py` keeps `verify=False` deliberately — it is a credential-free internal probe.
  - Turning **semgrep** on surfaced 6 more, all **XXE**: stdlib `xml.etree` parsing *untrusted* XML —
    the SAML assertion an attacker POSTs to the ACS endpoint, plus external threat/ArXiv feeds. External
    entities resolve, so this allowed local-file exfiltration, SSRF and billion-laughs DoS. Fixed with
    `defusedxml` at all 4 call sites (`auth/saml.py`, `brain/threat_feed.py`, `threat_intel/sources.py`),
    pinned in requirements, and pinned shut by `warden/tests/test_xxe_hardening.py`.
  - **Lesson:** semgrep and gitleaks were landed *observing* precisely because they could not be run on
    the dev box. That call was right — semgrep failed on its first run. Gating them sight-unseen would
    have reddened `main`. Both gate now, from a verified baseline.

## Immediate actions

1. Adopt the `SR-*` / `DE-*` prefixes; retire bare "Phase N" in messages.
2. ~~Merge SR-5 and DE-6~~ — done (DE-6 delivered both).
3. ~~Land #154 / GSAM chain~~ — #154 merged (1c920552).
4. ~~C1 regression test~~ — done (`test_causal_c1_guardrail.py`).
5. **SR-7 remainder:** all three SAST/secret gates now gate (bandit HIGH / semgrep ERROR / gitleaks).
   Still open: coverage floor 83% → 85% (SR-7.2 — the floor moved 75 → 79 → 83; the last 2 points are
   non-security routers). SR-7.3 is remediated on four modules and re-measured 2026-08-31
   (`causal_arbiter` 405 → 200 survivors); what is open is a fifth module and a CI runner for it.
6. **DE-7 remainder:** BrowserSandbox process isolation (seccomp/restricted-user sidecar).
7. **SR-2.3 transport wiring** — the validated-IP resolver (`resolve_validated_ips`) is done + tested;
   the remaining step is a pinned httpx transport (connect to the validated IP, preserve Host/SNI) wired
   into the live outbound callers. That step needs a real-host TLS-SNI test and auto-deploys to prod, so
   it was NOT shipped blind. (SR-1.4b resolved: own-tenant-or-admin.)
