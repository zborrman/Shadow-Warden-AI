# ── Python ────────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.pyo
*.pyd
*.egg
*.egg-info/
dist/
build/
.eggs/
*.whl
.Python

# ── Virtual environments ──────────────────────────────────────────────────────
.venv/
venv/
env/
.env
ENV/

# ── Secrets — NEVER commit these ─────────────────────────────────────────────
.env
*.env
secrets.json
credentials.json

# ── ML model weights (large binaries — pulled at build time) ─────────────────
warden/models/
models/

# ── Runtime data ──────────────────────────────────────────────────────────────
data/logs.ndjson
data/logs.json
data/dynamic_rules.json


# ── OS / editor ───────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
*.swp
*.swo
.idea/
.vscode/
*.code-workspace

# ── Test / coverage ───────────────────────────────────────────────────────────
.pytest_cache/
.coverage
htmlcov/
.mypy_cache/
.ruff_cache/

# ── mTLS certificates (generated by scripts/gen_certs.sh — NEVER commit) ─────
certs/*.key
certs/*.crt
certs/*.srl
certs/*.csr
certs/*.crl
certs/*.ext
certs/ca.cnf
certs/ca.db
certs/ca.db.attr
certs/issued/

# ── Playwright ────────────────────────────────────────────────────────────────
playwright-report/
test-results/
gtm/leads.csv

# Databases
data/*.db
data/*.db-shm
data/*.db-wal

# AI/IDE Cache
.claude/
=8.0.0

# Test / build artifacts
coverage.json
coverage*.json
shadow_warden_ai.egg-info/

# Misplaced assets
*.png
!site/public/*.png
!landing/**/*.png
!dashboard/public/*.png
!portal/public/*.png

# SSL certificates (generated on server, not committed)
ssl/

.vercel
.env.sepolia

# Build artifacts — never commit
dashboard/.next/
dashboard/node_modules/
dashboard/tsconfig.tsbuildinfo
dashboard/next-env.d.ts
dashboard/package-lock.json
worker/node_modules/
worker/package-lock.json
obsidian-plugin/node_modules/
obsidian-plugin/package-lock.json
sdk/typescript/node_modules/
.playwright-mcp/
portal/tsconfig.tsbuildinfo
data/evolution_dataset.jsonl
data/corpus_snapshot.json
.env.sepolia
