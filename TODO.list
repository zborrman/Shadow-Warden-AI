╔══════════════════════════════════════════════════════════════════════════════╗
║               Shadow Warden AI — Project TODO List                         ║
║               Version 4.13 · Updated 2026-05-06                            ║
╚══════════════════════════════════════════════════════════════════════════════╝

Priority legend:
  [!!!] Critical — blocks production or revenue
  [!!]  High — complete before next version bump
  [!]   Medium — Q2 2026 target
  [ ]   Low / Roadmap — post Q2 2026

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  1. BACKEND — OPEN ITEMS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

[!!]  FraudScore API backend — POST /fraud-score/evaluate
      → Landing page UI exists; backend stub returns 404
      → Integrate financial/impact_calculator.py + ERS score
      → Gate: Individual+ tier

[!!]  Agent Action Whitelist REST API (Q2.6)
      → warden/agentic/action_whitelist.py logic is complete
      → Needs FastAPI router warden/api/action_whitelist.py
      → CRUD: POST/GET/DELETE /admin/agents/{id}/whitelist

[!]   Shadow AI Dashboard for SMB (Q3.8)
      → Non-Streamlit, standalone HTML page in landing/
      → Show findings list + policy toggle + provider list
      → Requires shadow_ai_enabled feature gate check at load

[!]   Obsidian feed persistence
      → Currently feed returns last 20 scan results from in-memory list
      → Move to SQLite table sep_obsidian_feed in SEP_DB_PATH
      → Survives restarts, supports pagination offset

[!]   Secrets Vault add-on webhook handler
      → POST /billing/webhooks/lemon-squeezy grants secrets_vault add-on
      → Map product ID → add-on key → billing/addons.grant_addon()
      → Test with Lemon Squeezy sandbox

[ ]   NVIDIA NeMo / GeometricThreatBridge production wiring
      → Settings UI exists (SettingsSection.astro + settings.html)
      → Backend stub in warden/integrations/nvidia/
      → GeometricThreatBridge: translate hyperbolic dist + Betti → Colang vars
      → ColangEvolutionSynthesizer: auto-generate .co guardrail files

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  2. CI / QUALITY
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

[!!]  Coverage gate — restore to ≥75%
      → Recent additions (v4.9 secrets_gov, v4.10 obsidian) added uncovered lines
      → warden/tests/test_coverage_boost.py exists but needs expansion
      → Target: 76%+ without disabling any existing tests

[!]   mutmut — enforce ≤20 surviving mutants
      → Targets: secret_redactor.py + semantic_guard.py
      → Currently unblocked (CI runs mutmut informational only)
      → Add threshold check to CI on mutation-testing job

[!]   Hadolint GitHub Actions step
      → Lint all Dockerfile* files for best practices
      → Add hadolint/hadolint-action@v3 to ci.yml lint job

[!]   docker scout / Trivy image CVE scan
      → Add trivy-action after Docker smoke build
      → Gate on CRITICAL severity CVEs

[ ]   Obsidian plugin TypeScript CI
      → Add obsidian-plugin build step to ci.yml
      → npm install && npm run build → fail if compile errors
      → Check manifest.json version matches git tag on release

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  3. LANDING / FRONTEND
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

[!]   landing/index.html footer — update v4.7.0 → v4.10
      → Footer currently shows stale version badge

[!]   Obsidian landing page
      → New page landing/obsidian.html
      → Hero: "Your AI-Secured Vault" — plugin screenshot, install steps
      → CTA → Community Business tier pricing

[!]   Secrets Governance landing page stub
      → Expand existing landing/settings.html Secrets Governance panel
      → Add screenshot and "How It Works" section
      → Link from pricing page Community Business tier card

[ ]   FraudScore landing page (landing/fraud-score.html)
      → Demo calculator functional but backend POST /fraud-score/evaluate missing
      → Wire up once backend implemented

[ ]   Accessibility widget — verify ♿ button renders correctly on mobile
      → Test on iOS Safari + Android Chrome
      → Confirm z-index doesn't conflict with iOS Safari bottom toolbar

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  4. BLOCK L — NEXT SPRINT (v4.13 → v4.14)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

[!!!] DNS A record dash.shadow-warden-ai.com → 91.98.234.160
      → Add in Cloudflare DNS dashboard
      → SOC dashboard is live but unreachable until DNS propagates

[!!]  Analytics API live endpoints wired into SOC dashboard
      → dashboard/src/lib/api.ts calls real /analytics/* endpoints
      → Replace mock data in overview/page.tsx and threats/page.tsx
      → Requires warden/api/analytics_query.py REST adapter

[!!]  Dashboard auth gate
      → JWT or X-Api-Key header validation in dashboard Next.js middleware
      → middleware.ts → validate against WARDEN_DASHBOARD_KEY env
      → Return 401 / redirect to login for unauthenticated requests

[!]   Dashboard → docker compose managed (not standalone docker run)
      → Current: container started manually via docker run on server
      → Target: `docker compose up dashboard` via CI autodeploy
      → Requires SSH deploy script to pull + restart compose dashboard service

[!]   OTel sample rate tuning for high-traffic prod
      → Current: 1.0 (all traces sampled)
      → Target: 0.1 for ALLOW verdicts; 1.0 for HIGH/BLOCK
      → Tail-based sampling in OTel Collector config

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  5. INFRASTRUCTURE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

[!]   Server backup automation
      → Daily cron: pg_dump + tar data/ → MinIO warden-backups/
      → Retention: 7 daily + 4 weekly + 3 monthly (Grandfather-Father-Son)
      → Alert on backup failure via Slack webhook

[!]   TimescaleDB compression policy for probe_results
      → Compression already defined in migration 0010
      → Verify add_compression_policy is running on production
      → Check chunk sizes via timescaledb_information.chunks

[ ]   TimescaleDB compression policy for probe_results
      → Compression already defined in migration 0010
      → Verify add_compression_policy is running on production
      → Check chunk sizes via timescaledb_information.chunks

[ ]   Kubernetes Helm chart
      → Deploy, Service, ConfigMap, HPA, PVC for warden-models
      → Target: AWS EKS, Azure AKS, Google GKE

[ ]   Terraform module (cloud deploy)
      → VPC + ECS/EKS + RDS (PostgreSQL) + ElastiCache (Redis)
      → Output: warden endpoint URL, dashboard URL

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  5. COMPLIANCE & DOCS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

[!]   Update docs/dpia.md to v4.10
      → Add Obsidian note scanner data flows
      → Add Secrets Governance vault connector section
      → Update processing activity table

[!]   Update docs/soc2-evidence.md to v4.10
      → Add Secrets Governance control mapping (CC6.1, CC6.2)
      → Add Obsidian share gate as access control evidence
      → Update evidence collection procedures

[ ]   ISO 27001 gap analysis
      → Map existing controls to ISO 27001 Annex A requirements
      → Prioritise: A.8 (asset management), A.9 (access), A.12 (operations)

[ ]   EU AI Act compliance checklist
      → Shadow Warden classified as AI safety system (Art. 6 Annex III)
      → Document risk management system (Art. 9)
      → Document human oversight measures (Art. 14)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  6. DONE ✓ (v4.13 current)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

[✓]  Core gateway — 9-stage pipeline (TDA, obfuscation, secrets, semantic, ML, causal, phish, ERS, decision)
[✓]  Evolution Engine — Claude Opus auto-rule gen, hot-reload, Intel Bridge
[✓]  SOVA Agent — 30 tools, 7 ARQ cron jobs, WardenHealer, visual patrol
[✓]  MasterAgent — 4 sub-agents, HMAC tokens, human-in-the-loop (Pro)
[✓]  Shadow AI Governance — 18 providers, /24 subnet probe, DNS syslog sink (Enterprise / add-on)
[✓]  Explainable AI 2.0 — 9-stage CausalChain, HTML+PDF renderer (Pro / add-on)
[✓]  Sovereign AI Cloud — 8 jurisdictions, MASQUE tunnels, attestation (Enterprise)
[✓]  Post-Quantum Cryptography — HybridSigner Ed25519+ML-DSA-65, HybridKEM (Enterprise)
[✓]  SEP — UECIID, peering, Knock-and-Verify, Transfer Guard, Data Pods, STIX 2.1 chain
[✓]  Billing — 5-tier pricing, add-on catalog, Lemon Squeezy integration
[✓]  SMB stack — File Scanner, Email Guard, Compliance Report, Offline Mode
[✓]  Uptime Monitor — TimescaleDB hypertable, continuous aggregate, WebSocket
[✓]  Financial Impact Calculator — IBM 2024 benchmarks, ROI tiers, CLI
[✓]  Secrets Governance v4.9 — 5 vault connectors, inventory, lifecycle, policy, 14 endpoints, Streamlit dashboard
[✓]  Obsidian Integration v4.10 — note scanner, 5 API endpoints, TypeScript plugin, auto-scan, UECIID share
[✓]  Accessibility widget — ♿ international symbol, click bug fixed, main page only (v4.10)
[✓]  Enterprise Integration Guide — updated to v4.10 (all pillars, Obsidian section, Settings link)
[✓]  Enterprise Settings — updated to v4.10 (Secrets Gov, Obsidian, Accessibility panels)
[✓]  NVIDIA / NeMo Settings UI — SettingsSection.astro + settings.html panels
[✓]  Fail-closed auth — startup RuntimeError if no API key (unless ALLOW_UNAUTHENTICATED=true)
[✓]  VAULT_MASTER_KEY Fernet validation on startup
[✓]  Shadow ban randomness — secrets.choice(), 30+ gaslight pool entries
[✓]  CPT drift gate — rejects CausalArbiter CPT updates >25% from prior
[✓]  Evolution regex gate — compile + timeout + nested-quantifier heuristic
[✓]  OTel Distributed Tracing — TracerProvider, gRPC exporter, per-layer spans in all 9 stages, OTel Collector, Jaeger 1.58
[✓]  SOC Next.js Dashboard — Next.js 14.2 App Router, TanStack Query, Recharts, Tailwind dark theme, 8 pages, Docker service, Caddy vhost
[✓]  CI / Lint / Type Hardening — 7 ruff errors fixed, mypy attr-defined fixed, --no-cache pre-build, python3 -m pip, User-Agent probe header
[✓]  CI — test matrix 3.11/3.12, lint, Docker smoke, mutation testing
[✓]  Prometheus + Grafana SLO alerts (P99, 5xx, shadow ban rate, corpus drift)
[✓]  SIEM integration — Splunk HEC + Elastic ECS
[✓]  LangChain callback (WardenCallback)
[✓]  Production server on Hetzner VPS — api.shadow-warden-ai.com
[✓]  GitHub Actions CI autodeploy → server

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
